export const selected = $state({
	color: 'red'
});

export const bucket = $state({
	color: ''
});

export const brush = $state({
	size: 10
});

export const pages = $state({
	number: 1
});

export const selectionMode = $state({
	active: false
});